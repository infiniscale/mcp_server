# === mcp-convert-router 环境变量模板 ===
# 使用方式：
# 1) 复制一份：cp .env.template .env
# 2) 按需修改 .env
# 3) 在启动 MCP Server 前加载环境变量（或由你的进程管理器/IDE 注入）

# --- 日志 ---
# 日志等级：DEBUG / INFO / WARNING / ERROR
MCP_CONVERT_LOG_LEVEL=INFO

# --- 工作目录 ---
# 临时工作目录（每次请求会创建 work_dir 子目录）
MCP_CONVERT_TEMP_DIR=/tmp/mcp-convert
# 临时目录保留时间（小时），超时会被清理
MCP_CONVERT_RETENTION_HOURS=24

# --- 服务端口（仅当你后续增加 HTTP/SSE 传输时使用；stdio 模式可忽略）---
MCP_CONVERT_HOST=127.0.0.1
MCP_CONVERT_PORT=8002
MCP_CONVERT_TRANSPORT=stdio

# --- 输入/安全限制 ---
# 最大文件大小（MB）
MCP_CONVERT_MAX_FILE_MB=50
# croc 接收超时（秒）
MCP_CONVERT_CROC_TIMEOUT_SECONDS=120

# 允许本地 file_path 输入的根目录白名单（逗号分隔，建议必须设置）
# 例：/Users/rrong/Documents,/tmp/mcp-convert
MCP_CONVERT_ALLOWED_INPUT_ROOTS=
# 是否强制要求必须配置白名单（true/false）
MCP_CONVERT_REQUIRE_ALLOWLIST=true

# --- MinerU（建议二选一：远程 or 本地）---
# 远程 MinerU API（云服务）
MINERU_API_BASE=https://mineru.net
MINERU_API_KEY=

# 本地 MinerU API（若你部署了本地服务）
USE_LOCAL_API=false
LOCAL_MINERU_API_BASE=http://localhost:8080

# MinerU 轮询/超时（秒）
MINERU_TIMEOUT=300
MINERU_POLL_INTERVAL_SECONDS=5

# MinerU 结果 zip 最大允许下载大小（字节）
# 注意：这是“下载结果”大小上限，不是原始输入文件大小上限
MINERU_MAX_RESULT_ZIP_BYTES=209715200
